PORT=3000
NODE_ENV=development

# Database (individual vars — used in local development)
DB_HOST=127.0.0.1
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=martecommerce_db
DB_PORT=3306
DB_CONNECTION=mysql

# Database (connection URL — used in production deployments, overrides individual vars)
# DATABASE_URL=mysql://user:password@host:3306/dbname

# Frontend URL (CORS)
ORIGIN=http://localhost:5173
